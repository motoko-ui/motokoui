{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "envMode": "strict",
  "globalEnv": ["NODE_ENV", "NEXT_PUBLIC_APP_URL", "GITHUB_TOKEN"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["NEXT_PUBLIC_APP_URL"],
      "outputs": ["dist/**", ".next/**", "public/r/**"]
    },
    "preview": {
      "env": ["NEXT_PUBLIC_APP_URL"],
      "outputs": [".next/**"]
    },
    "start": {
      "dependsOn": ["^build"]
    },
    "lint": { "cache": false, "outputs": [] },
    "lint:fix": { "cache": false, "outputs": [] },
    "format:check": { "cache": false, "outputs": [] },
    "format:fix": { "cache": false, "outputs": [] },
    "typecheck": {},
    "dev": { "cache": false },
    "check": { "cache": false },
    "test": { "cache": false, "outputs": [] }
  }
}
